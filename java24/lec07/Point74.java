public class Point74 {
  double x;
  double y;

  // フィールドx，yに0.0，0.0を代入する
  // コンストラクタ名はPoint74に変更すること
  public Point74() {
    x = 0.0;
    y = 0.0;
  }

  // フィールドx，yにdouble型のa，bを代入
  // コンストラクタ名はPoint74に変更すること
  public Point74(double a, double b) {
    x = a;
    y = b;
  }
}
